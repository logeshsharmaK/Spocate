enum AddCarNavFrom { Login, OTP, MyCars , MyCarsSourceDestination }

enum DialogAction { Yes, No, OK, Cancel }

enum DialogActionControl { SHOW, HIDE }

enum IsSocialLogin { Yes, No }

enum CarInfoAnimState { reverse , forward }

enum SearchDestinationSubmits { ADD, REMOVE, SUBMIT }

enum SearchDestinationFrom { FROMFAVORITE, FROMSEARCH, FROMRECENT }
// enum LoginType { google, facebook, apple, phone_number, email }
